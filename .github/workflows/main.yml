name: AI Daily News Auto Run

on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *' # 北京时间 10:00

jobs:
  run_daily_news:
    runs-on: ubuntu-latest
    
    steps:
    - name: Check out code
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9' # 🚨 回退到 3.9，这是 Google 库支持最好的版本
        
    - name: Install dependencies
      # 🚨 重点修改：
      # 1. 删除了 "upgrade pip" (不再手动升级，使用系统自带的稳定版)
      # 2. 增加了 setuptools 和 wheel 的升级 (防止编译失败)
      run: |
        python -m pip install --upgrade setuptools wheel
        pip install google-generative-ai requests beautifulsoup4 python-dotenv
        
    - name: Run script
      env:
        GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
      run: python main.py
